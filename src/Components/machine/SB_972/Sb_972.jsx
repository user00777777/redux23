import React from 'react'
// import s from ''
import { useSelector } from 'react-redux'

export default function Sb_972() {
	const res = useSelector(state => state)
	console.log(res)

	return <div>sb_972</div>
}
